---
description: Launch one task in a fresh agent session, worktree, and pane in this window
---

Load the `orchestrating-work` skill and run its `/spawn-work` flow.
Use `$ARGUMENTS` as the task, optional agent, and base ref.
Use the caller's agent unless another is named. Never reuse an old session.

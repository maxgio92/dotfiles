---
description: Coordinate a plan across agents in separate worktrees and panes, with a live task table
---

Load the `orchestrating-work` skill and run its `/orchestrate` flow.
Use `$ARGUMENTS` as the plan or plan path. If empty, use the current agreed plan.
Keep the current session as coordinator. Start monitoring after adoption or launch.
